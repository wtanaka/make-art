.app-width: .workspace
    .app-container
      display(
          source='appCode',
          workspace,
          mode='mode',
          shareid="id",
          trigger="trigger",
          ng-style="{'max-width': iframe ? '100%': '500px'}",
          ng-if="!loading"
          )
    
