{
    "id": "umbrella",
    "title": "Do I need an umbrella?",
    "cover": "umbrella.gif",
    "description": "Draw a balloon floating in the air using polygons!",
    "startAt": 2,
	"trigger": 1,
    "steps": [
        {
            "hint": "We can build a useful tool using data that will tell us if we need an umbrella before we go out. If it is raining, it will tell us to bring an umbrella. Let’s get the weather by **typing** `weather = get 'weather', location`",
            "solution": "weather = get 'weather', location"
        },
        {
            "hint": "Move up 50 to get into position for writing your question. **Type** `move 0, -50`",
            "solution": "move 0, -50"
        },
        {
            "hint": "Write your text. **Type** `text 'DO I NEED AN UMBRELLA NOW?'`",
            "solution": "text \\'DO I NEED AN UMBRELLA NOW\\?\\'"
        },
        {
            "hint": "Now move down to get into position for writing the answer with `move 0, 150`",
            "solution": "move 0, 150"
        },
        {
            "hint": "The answer should be big, lets use `font 110`",
            "solution": "font 110"
        },
        {
            "hint": "If it is raining, we want to tell the user to grab an umbrella. To do this, we're going to use the `if` word. **Type** `if weather.condition.text is 'Rain'`",
            "solution": "if weather.condition.text is 'Rain'"
        },
        {
            "hint": "If it is raining, then we want to tell the user with text and an emoji. Add the emoji by copying and pasting here: `then text 'YES ☔️'`",
            "solution": "    text 'YES ☔️'"
        },
        {
            "hint": "If it is not raining, we will just write different text. Press `BACKSPACE` to get out of the if block, and then copy and paste: `else text 'NO 😎'`. (Make sure there isn't any space in front of it).",
            "solution": "else text 'NO 😎'"
        },
        {
            "hint": "We can put a little bit more information down at the bottom of the screen. **Type** `move 0, 130`",
            "solution": "move 0, 130"
        },
        {
            "hint": "It shouldn't be the focus of the app, so let’s bring down the font size. **Type** `font 20`",
            "solution": "font 20"
        },
        {
            "hint": "Put the temperature and condition in with **Type** `text weather.condition.temp + 'F and ' + weather.condition.text`",
            "solution": "text weather.condition.temp \\+ \\'F and \\' \\+ weather.condition.text"
        }
    ],
    "completion_text": "Amazing, what other tools can you make with data?"
}
